export default function clickBreakpoint (el: HTMLElement, pos: number) {
       // let breakpointPos = Math.round((pos) / widthCanvas * 100);
       // if (breakpointPos >= 50) {
       //     setMaxDisplayValue(breakpointPos);
       // } else {
       //     setMinDisplayValue(breakpointPos);
       // }
   }